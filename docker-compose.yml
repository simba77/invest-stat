version: '3'
services:
  webserver:
    build: docker/general
    container_name: ubuntu.${APP_NAME}
    depends_on:
      - mariadb
    volumes:
      - .:/var/www/html
    ports:
      - '${NGINX_HTTP_PORT:-8000}:80'
      - '${NGINX_HTTP_PORT:-8001}:443'
    networks:
      - web
      - default
    labels:
      - traefik.enable=true
      - traefik.http.routers.http-${APP_NAME}.rule=Host(`${APP_HOST}`)
      - traefik.http.routers.https-${APP_NAME}.rule=Host(`${APP_HOST}`)
      - traefik.http.routers.https-${APP_NAME}.tls=true
  mariadb:
    image: 'mariadb:10'
    container_name: mariadb.${APP_NAME}
    ports:
      - '${FORWARD_DB_PORT:-3306}:3306'
    environment:
      MYSQL_ROOT_PASSWORD: '${DB_PASSWORD:-laravel}'
      MYSQL_ROOT_HOST: "%"
      MYSQL_DATABASE: '${DB_DATABASE:-laravel}'
      MYSQL_USER: '${DB_USERNAME:-laravel}'
      MYSQL_PASSWORD: '${DB_PASSWORD:-secret}'
      MYSQL_ALLOW_EMPTY_PASSWORD: 'yes'
    volumes:
      - './docker/mariadb/data:/var/lib/mysql'
    networks:
      - default
    labels:
      - traefik.enable=false
  phpmyadmin:
    image: phpmyadmin
    container_name: phpmyadmin.${APP_NAME}
    restart: always
    ports:
      - 8081:80
    environment:
      - PMA_ARBITRARY=0
      - PMA_HOST=${DB_HOST}
    networks:
      - web
      - default
    labels:
      - traefik.enable=true
      - traefik.http.routers.phpmyadmin-${APP_NAME}.rule=Host(`phpmyadmin.loc`)
  redis:
    build: docker/redis
    container_name: redis.${APP_NAME}
    volumes:
      - ./docker/redis/data:/data
    ports:
      - '${REDIS_PORT:-6379}:${REDIS_PORT:-6379}'
    networks:
      - default
    labels:
      - traefik.enable=false

networks:
  web:
    external:
      name: traefik_default
